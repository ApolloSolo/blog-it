{{!-- <div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div> --}}

{{#each this}}
<div class="card mb-3">
  <div class="col-md-8">
    <div class="card-body">
      <h5 class="card-title">
        {{user.username}} on {{format_date created_at}}
      </h5>
      <p class="card-text">
        {{comment_text}}
      </p>
    </div>
  </div>
</div>
{{/each}}